################################################################################
# OES GenAI Micro-Apps – Requirements File
# Maintained by: Imaad Fakier (Senior GenAI Developer)
#
# Context:
#     Dependency layer for the Canvas Import / DOCX → Canvas Streamlit application.
#     Pinned versions ensure stability locally and on Streamlit Community Cloud.
#
# RULES:
#   • Pin EVERYTHING (Streamlit Cloud requires reproducibility).
#   • Only include libraries ACTUALLY used by this micro-app.
#   • If you add a new library, pin it and document why.
################################################################################


# ------------------------------------------------------------------------------
# CORE STREAMLIT RUNTIME
# ------------------------------------------------------------------------------
streamlit==1.39.0                 # stable + compatible with community cloud
python-dotenv==1.0.1              # optional .env support for local development
requests==2.32.3                  # HTTP client for Canvas API + external calls
Pillow==10.4.0                    # image handling (Streamlit dependency)


# ------------------------------------------------------------------------------
# DOCUMENT PROCESSING
# ------------------------------------------------------------------------------
python-docx==1.1.2                # parsing .docx storyboards/modules


# ------------------------------------------------------------------------------
# GOOGLE DRIVE / GOOGLE DOCS INTEGRATION
# Needed for: fetch_docx_from_gdoc(), outline extraction, bookmarks, headings.
# ------------------------------------------------------------------------------
google-api-python-client==2.133.0
google-auth==2.29.0
google-auth-httplib2==0.2.0


# ------------------------------------------------------------------------------
# OPENAI SDK
# Notes:
#   - Supports GPT-4o, GPT-4o-mini, file_search, vector stores.
#   - Version pinned to avoid breaking changes in major releases.
# ------------------------------------------------------------------------------
openai==1.13.0


# ------------------------------------------------------------------------------
# TYPE SUPPORT / INTERNAL OPTIMIZATIONS (Implicit)
# These are indirect dependencies pulled by modules above, pinned here to avoid
# future breakage on Streamlit cloud. DO NOT remove.
# ------------------------------------------------------------------------------
protobuf==5.27.3                  # required by google API client stack
httplib2==0.22.0                  # google-auth-httplib2 dependency
